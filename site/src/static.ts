import fs from "fs"

const CONTENT_DIR = "static-content"
const OUTPUT_DIR = "static-build"
const pages = []

// Load all into memory
function loadPagesInDirectory(path: string) {
}

// Apply pages to templates and save