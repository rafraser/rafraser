{
  "name": "rafraser",
  "version": "1.0.0",
  "description": "Yet another static site generator",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run lint",
    "lint": "tsc --noEmit",
    "static-build": "npm run static-sass & node dist/static.js",
    "static-clean": "rm -rf static-build",
    "static-sass": "sass static-content/sass:static-build/assets/style",
    "static-all": "npm run static-clean && npm run static-build"
  },
  "keywords": [],
  "author": "Robert A Fraser",
  "license": "ISC",
  "dependencies": {
    "marked": "^1.2.7",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "@types/marked": "^1.2.1",
    "@types/node": "^14.14.14"
  }
}
